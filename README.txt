** Instructions on how to compile and run movies_by_year.c:
1. Ensure the sample csv files are in the same directory as movies_by_year.c
2. Compile movies_by_year.c using "gcc --std=c99 -o movies_by_year movies_by_year.c" to compile the executable file
3. Run movies_by_year using "./movies_by_year"
